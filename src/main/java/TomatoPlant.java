public class TomatoPlant extends EdibleTomato{
    TomatoPlant yield(boolean hasBeenFertilized, boolean hasBeenHarvested) {
        if (hasBeenHarvested == true && hasBeenHarvested == true) {

            return this;
        }
        return null;
    }
}
